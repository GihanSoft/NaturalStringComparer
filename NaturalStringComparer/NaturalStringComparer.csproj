<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>netstandard2.0</TargetFramework>
    <RootNamespace>Gihan.Helpers.String</RootNamespace>
    <Authors>Muhammad Babayi</Authors>
    <Company>GihanSoft</Company>
    <Description>Compare for string to sort by number in string (as windows explorer do)</Description>
    <Copyright>Copyright (c) 2018 Mohammad Babayi</Copyright>
    <PackageLicenseUrl></PackageLicenseUrl>
    <PackageProjectUrl>https://github.com/chiefmb/NaturalStringComparer</PackageProjectUrl>
    <RepositoryUrl>https://github.com/chiefmb/NaturalStringComparer</RepositoryUrl>
    <RepositoryType>git</RepositoryType>
    <PackageRequireLicenseAcceptance>true</PackageRequireLicenseAcceptance>
    <PackageId>Gihan.Helpers.StringHelper.NaturalStringComparer</PackageId>
    <Version>1.5.1</Version>
    <PackageReleaseNotes>bug fixed</PackageReleaseNotes>
    <PackageTags>string, comparer, IComparer, NaturalComparer, NaturalStringComparer</PackageTags>
    <GeneratePackageOnBuild>true</GeneratePackageOnBuild>
    <PackageLicenseFile>LICENSE</PackageLicenseFile>
    <AssemblyName>Gihan.NaturalStringComparer</AssemblyName>
    <ApplicationIcon>gihansoft.ico</ApplicationIcon>
    <PackageIconUrl>https://raw.githubusercontent.com/chiefmb/NaturalStringComparer/NaturalStringComparer/master/gihansoft.ico</PackageIconUrl>
  </PropertyGroup>

  <ItemGroup>
    <None Include="..\LICENSE">
      <Pack>True</Pack>
      <PackagePath></PackagePath>
    </None>
  </ItemGroup>

</Project>
